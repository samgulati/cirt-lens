import {useCallback,useEffect,useState} from 'react';import {useNavigate} from 'react-router-dom';import {api} from '../api/client';import {EmptyState,ErrorPanel,IncidentTable,Loading,PageTitle} from '../components/common';import type {Incident} from '../types';
export default function IncidentsPage(){const nav=useNavigate(),[items,setItems]=useState<Incident[]>(),[error,setError]=useState('');const load=useCallback(()=>{setError('');api<Incident[]>('/incidents').then(setItems).catch((e:Error)=>setError(e.message))},[]);useEffect(load,[load]);if(error)return <ErrorPanel message={error} retry={load}/>;if(!items)return <Loading/>;return <><PageTitle eyebrow="Case Management" title="Incidents" sub="Correlated investigations prioritized by risk and evidence confidence"/>{items.length?<IncidentTable items={items} onOpen={id=>nav('/incidents/'+id)}/>:<EmptyState message="No incidents have been correlated yet."/>}</>}
