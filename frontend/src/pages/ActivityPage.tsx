import {useCallback,useEffect,useState} from 'react';import {Command} from 'lucide-react';import {api} from '../api/client';import {ErrorPanel,Loading,PageTitle,fmt} from '../components/common';import type {ActivityRecord} from '../types';
export default function ActivityPage(){const[items,setItems]=useState<ActivityRecord[]>(),[error,setError]=useState('');const load=useCallback(()=>{void api<ActivityRecord[]>('/activity').then(setItems).catch((e:Error)=>setError(e.message))},[]);useEffect(load,[load]);if(error)return <ErrorPanel message={error} retry={load}/>;if(!items)return <Loading/>;return <><PageTitle eyebrow="Audit & Compliance" title="Activity Log" sub="Immutable-style trail of every simulated analyst action"/><div className="panel overflow-hidden"><div className="grid-table grid-cols-[160px_150px_1fr_130px_100px] px-4 label bg-[#0b1924]"><span>Timestamp</span><span>Analyst</span><span>Action</span><span>Incident</span><span>Result</span></div>{items.map(item=><div key={item.id} className="grid-table grid-cols-[160px_150px_1fr_130px_100px] px-4 text-xs"><span>{fmt(item.timestamp)}</span><span>{item.analyst}</span><span className="font-mono">{item.action}</span><span className="text-cyan-300">{item.incident_id}</span><span className="text-emerald-300">{item.result}</span></div>)}{!items.length&&<div className="p-12 text-center"><Command className="mx-auto muted"/><div className="mt-3">No analyst actions yet</div><div className="text-xs muted mt-1">Execute a response action to create an audit entry.</div></div>}</div></>}
